version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/query"
    schema: "sql/schema.sql"
    gen:
      go:
        package: "sqlgen"
        out: "outbound/sqlgen"
        sql_package: "pgx/v5"
